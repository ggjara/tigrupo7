<% content_for :header do %>
    <h1>Business Intelligence: Financieros</h1>
<%end%>

<% content_for :content do %>
    <div class="box">
      <div class="box-header with-border">
        <h3 class="box-title">TRANSACCIONES</h3>
      </div>
      <div class="box-body">
        <table class="table">
          <thead>
          <tr>
            <th>Hora</th>
            <th>Cargo</th>
            <th>Abono</th>
            <th>Cuenta involucrada</th>
          </tr>
          </thead>
          <tbody>
            <tr>
              <% Trx.where(fechaCreacion: params[:id]).each do |trx|%>
                <td><%= trx.fechaCreacion.strftime("%H:%M") %></td>
                <% if trx.cuentaOrigen == "" %>
                  <td><%= trx.monto %></td>
                  <td></td>
                  <td> <%= trx.cuentaDestino %></td>
                <% else %>
                  <td></td>
                  <td><%= trx.monto %></td>
                  <td> <%= trx.cuentaOrigen %></td>
                <% end %>
              t<% end %>
            </tr>
          </tbody>
        </table>
      </div>
      <div class="box-footer">
      </div><!-- /.box-footer-->
    </div>
<% end %>